group shared() {
    @install()
    fun print_name_with_title() {
        if(!SOULS_MEMORY.CHEAT_COUNTER_NEVER_LOADED) {
            text("[BOY], the cheater");
        } else if(False) {
            text("sir [BOY]");
        } else {
            text("stranger");
        }
    }
};
