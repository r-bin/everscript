group shared() {
    enum AREA_BASEMENT {
        DEBUG = False,

        // ENABLE_DOORS = memory(WORD, TEMP),

        ENEMY_COUNT_TOTAL = memory(WORD, TEMP_RESERVED),
        ENEMY_COUNT = memory(WORD, TEMP_RESERVED),

        MUSIC_DEFAULT = MUSIC.PUPPET_SHOW,
    }
};
