group shared() {
    enum PIPE_MAZE {
        MARIO_BOSS_ROOM_OPEND = memory(FLAG, SRAM),
        HIDDEN_ROOM_OPEND = memory(FLAG, SRAM),

        MUSIC_ENTRANCE = 0x72,
        MUSIC_MAZE = 0x72,
    }
};
