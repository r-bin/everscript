group shared() {
    enum AREA_TEMPLE {
        MUSIC = MUSIC.HALLS_1,

        // level 0
        NORTHERN_WALL = 0d408,
        SWITCH_MINTAUR = memory(FLAG, SRAM),
        SWITCH_BRIDGE = memory(FLAG, SRAM),

        // level 1
        UNLOCKED_LEVEL_1 = memory(FLAG, SRAM),

        // level 2
        UNLOCKED_LEVEL_2 = memory(FLAG, SRAM),
    }
};
