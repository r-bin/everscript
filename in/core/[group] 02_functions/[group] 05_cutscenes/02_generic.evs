group generic() {
    @install()
    @async()
    @weak()
    fun generic_impact(duration) {
        sfx_effect(SHAKING, True);
        sound(IMPACT);
        
        sleep(duration);

        sfx_effect(SHAKING, False);
    }
};
